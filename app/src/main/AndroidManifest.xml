<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.whatsup.hawre.whatsup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="activities.WheelMenuActivity" />
        <activity android:name="activities.ListViewActivity" />
        <activity android:name="items.ListViewRow" />
        <activity android:name="activities.GridViewActivity" />
        <activity android:name="items.GridViewItem" />
        <activity android:name="activities.TopicActivity" />
        <activity android:name="items.SliderImageItem" />
        <activity android:name="activities.MainMenuActivity" />
        <activity android:name="activities.RecyclerViewActivity" />
        <activity android:name="items.RecyclerViewItem" />
        <activity android:name="activities.HorizontalRecyclerView" />
        <activity android:name="items.HorizontalRecyclerViewItem" />
        <activity android:name="activities.InfinitViewActivity" />
        <activity android:name="items.InfinitViewItem" />
        <activity android:name="activities.ImageSliderView" />
        <activity android:name="items.activity_comment_list_item"></activity>
    </application>

</manifest>